name: 🛡️ Decision Database CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # 1. Frontend & Dashboard (Next.js)
  frontend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      # Prepare local dependencies
      - name: Install tempus-node
        working-directory: Tempus-Engine/tempus-node
        run: npm install --legacy-peer-deps

      # Build Dashboard
      - name: Install Dashboard dependencies
        working-directory: Tempus-Engine/tempus-dashboard
        run: npm install --legacy-peer-deps
      
      - name: Build Dashboard
        working-directory: Tempus-Engine/tempus-dashboard
        run: |
          export CI=false
          export NEXT_TELEMETRY_DISABLED=1
          npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8001
          NEXT_PUBLIC_SEMANTIC_API_URL: http://localhost:8000

  # 2. Governance Kernel (Python & Rust)
  backend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python deps
        run: |
          cd Tempus-Engine
          python -m pip install --upgrade pip
          pip install ruff pytest
      - name: Lint with Ruff (Non-blocking for now)
        run: |
          cd Tempus-Engine
          # Run check but don't fail the build if minor issues found
          ruff check . --exit-zero

  # 3. Docker Compose (Infrastructure Integrity)
  docker-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Docker Compose config
        run: docker compose config
